name: CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  api-tests:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgis/postgis:15-3.4
        env:
          POSTGRES_DB: os
          POSTGRES_USER: bats
          POSTGRES_PASSWORD: bats
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U bats -d os"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
          --health-start-period 15s

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: api/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r api/requirements.txt
          pip install pytest fakeredis

      - name: Create .bats.cfg from template
        run: envsubst < docker/batapp/bats.cfg.template > ~/.bats.cfg
        env:
          DATABASE_HOST: localhost
          DATABASE_PORT: 5432
          DATABASE_NAME: os
          DATABASE_USER: bats
          DATABASE_PASSWORD: bats

      - name: Run API tests
        run: pytest api/test/ -v
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: os
          DB_USER: bats
          DB_PASSWORD: bats
          DATABASE_HOST: localhost
          DATABASE_PORT: 5432
          DATABASE_NAME: os
          DATABASE_USER: bats
          DATABASE_PASSWORD: bats
          CELERY_BROKER_URL: memory://
          CELERY_RESULT_BACKEND: cache+memory://
          PIPELINE_WORK_DIR: /tmp/circuitscape-test

  frontend-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Build WASM
        working-directory: frontend/wasm-connectivity
        run: |
          curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
          rustup target add wasm32-unknown-unknown
          make build
      - name: Build engine
        working-directory: frontend/gsbio-engine
        run: npm ci && npm run build
      - name: Build frontend
        working-directory: frontend
        run: npm ci && npm run build

  build-images:
    runs-on: ubuntu-latest
    needs: [frontend-build]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build API image (cache)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.backend
          tags: dispersion-prediction-app-api
          load: true
          cache-from: type=gha,scope=api
          cache-to: type=gha,scope=api,mode=max

      - name: Build frontend image (cache)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.frontend
          tags: dispersion-prediction-app-frontend
          load: true
          cache-from: type=gha,scope=frontend
          cache-to: type=gha,scope=frontend,mode=max

      - name: Build postgis image (cache)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.gis
          tags: dispersion-prediction-app-postgis
          load: true
          cache-from: type=gha,scope=postgis
          cache-to: type=gha,scope=postgis,mode=max

  integration-tests:
    runs-on: ubuntu-latest
    needs: [build-images]
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Restore API image from cache
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.backend
          tags: dispersion-prediction-app-api
          load: true
          cache-from: type=gha,scope=api

      - name: Restore frontend image from cache
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.frontend
          tags: dispersion-prediction-app-frontend
          load: true
          cache-from: type=gha,scope=frontend

      - name: Restore postgis image from cache
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile.gis
          tags: dispersion-prediction-app-postgis
          load: true
          cache-from: type=gha,scope=postgis

      - name: Create .env from example
        run: cp .env.example .env

      - name: Create .bats.cfg from template
        run: envsubst < docker/batapp/bats.cfg.template > ~/.bats.cfg
        env:
          DATABASE_HOST: localhost
          DATABASE_PORT: 5432
          DATABASE_NAME: os
          DATABASE_USER: bats
          DATABASE_PASSWORD: bats

      - name: Run Docker API integration tests
        run: SKIP_BUILD=true bash scripts/run-docker-api-test.sh

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pipeline-results
          path: tmp/api-output/
          retention-days: 7
